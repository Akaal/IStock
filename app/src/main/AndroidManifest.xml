<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mb.electronics.istock">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.DashboardActivity">

        </activity>
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".database.IStockProvider"
            android:authorities="@string/content_authority"
            android:exported="false" />

        <activity android:name=".activities.SearchProductsActivity" />
        <activity android:name=".activities.AddProductsActivity" />
        <activity android:name=".activities.EditProductsActivity" />
        <activity android:name=".activities.MiscActivity"></activity>
    </application>

</manifest>
